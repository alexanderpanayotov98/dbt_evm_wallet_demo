version: 2
models:
  - name: stg_wallets
    tests: [unique: {column_name: wallet_id}, not_null: {column_name: wallet_id}]
  - name: stg_events
    tests:
      - not_null:
          column_name: wallet_id
      - not_null:
          column_name: event_ts
