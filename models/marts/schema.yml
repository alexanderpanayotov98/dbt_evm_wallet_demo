version: 2
models:
  - name: mart_wallet_funnel
    description: Wallet-level funnel (deposit→first wager).
    tests:
      - not_null: {column_name: wallet_id}
  - name: mart_igaming_kpis
    description: Daily KPIs (GGR, NGR, ARPU).
    columns:
      - name: ngr
        tests: [not_null]
exposures:
  - name: tableau_workbook_evmiGaming
    type: dashboard
    maturity: low
    url: https://tableau-public-link-once-ready
    depends_on:
      - ref('mart_wallet_funnel')
      - ref('mart_igaming_kpis')
      - ref('mart_retention')
    owner:
      name: Alexander Panayotov
      email: alexanderpanayotov98@gmail.com
